---
type: inbox
status: active
area: capture
tags:
  - foc/inbox
---

# Inbox

临时想法先放这里。每天结束时只做三种处理：

1. 变成学习卡片，移动到 `notes/10_learning/`。
2. 变成问题闭环，移动到 `notes/40_debug/`。
3. 变成正式项目事实，回写到 `docs/`、`experiments/` 或 `interfaces/`。

不要让 Inbox 变成第二个资料堆。
